<?php
// Optional mail configuration for PHPMailer SMTP
// Copy this file to set real values in your environment.

// Example using Gmail SMTP (not recommended for production without app password)
// define('SMTP_HOST', 'smtp.gmail.com');
// define('SMTP_AUTH', true);
// define('SMTP_USERNAME', 'your-email@gmail.com');
// define('SMTP_PASSWORD', 'your-email-password-or-app-password');
// define('SMTP_SECURE', 'tls');
// define('SMTP_PORT', 587);

// For local dev, you can leave this file absent and the code will fallback to mail().

return true;
